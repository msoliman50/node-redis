// query builder

// const query = Model.findOe();
// query.limit(10);
// query.where('age').gt(17).lt(50);
// query.select('name age');

// execute the query
// query.exec(callback)

// or
// query.then(result => console.log(result));

// or
// const result = await query;